# ===============================
# Database
# ===============================
spring.datasource.url=${DB_URL}
spring.datasource.username=${DB_USERNAME}
spring.datasource.password=${DB_PASSWORD}

spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.format_sql=true
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.PostgreSQLDialect

# ===============================
# Server
# ===============================
server.port=8083

# ===============================
# File Upload
# ===============================
spring.servlet.multipart.max-file-size=10MB
spring.servlet.multipart.max-request-size=10MB

# ===============================
# OAuth2 - Azure (Microsoft) [DISABLED]
# ===============================
# spring.security.oauth2.client.provider.azure.issuer-uri=${issuer-uri}
# spring.security.oauth2.client.provider.azure.authorization-uri=https://login.microsoftonline.com/common/oauth2/v2.0/authorize
# spring.security.oauth2.client.provider.azure.token-uri=https://login.microsoftonline.com/common/oauth2/v2.0/token
# spring.security.oauth2.client.provider.azure.jwk-set-uri=https://login.microsoftonline.com/common/discovery/v2.0/keys
# spring.security.oauth2.client.provider.azure.user-info-uri=https://graph.microsoft.com/oidc/userinfo
# spring.security.oauth2.client.provider.azure.user-name-attribute=name
#
# spring.security.oauth2.client.registration.azure-dev.provider=azure
# spring.security.oauth2.client.registration.azure-dev.client-id=${client-id}
# spring.security.oauth2.client.registration.azure-dev.client-secret=${client-secret}
# spring.security.oauth2.client.registration.azure-dev.scope=openid,email,profile
# spring.security.oauth2.client.registration.azure-dev.redirect-uri={baseUrl}/login/oauth2/code/azure-dev

# ===============================
# Frontend
# ===============================
app.frontend.url=${FRONTEND_URL}
# app.oauth2.redirectUri=${app.frontend.url}/dashboard

# ===============================
# JWT
# ===============================
jwt.secret=${JWT_SECRET}
jwt.expiration=${JWT_EXPIRATION}

# ===============================
# Mail (disabled) + OAuth2 client (Microsoft disabled)
# ===============================
spring.autoconfigure.exclude=org.springframework.boot.autoconfigure.mail.MailSenderAutoConfiguration,org.springframework.boot.autoconfigure.security.oauth2.client.servlet.OAuth2ClientAutoConfiguration
